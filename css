/* ============================================================
   BASE
   ============================================================ */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --dark:   #12141D;
  --dark2:  #1a1d2b;
  --green:  #a3e635;
  --white:  #ffffff;
  --light:  #f9fafb;
  --muted:  #6b7280;
  --border: #e5e7eb;
  --bdark:  #2a2d3a;
}

html { scroll-behavior: smooth; }

body {
  font-family: 'Sora', sans-serif;
  background: var(--white);
  color: var(--dark);
  line-height: 1.6;
  overflow-x: hidden;
}

a { text-decoration: none; color: inherit; }

/* Wrapper */
.wrap {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 40px;
}

/* ============================================================
   BUTTONS
   ============================================================ */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 13px 28px;
  border-radius: 8px;
  font-family: 'Sora', sans-serif;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all .2s;
  border: none;
  white-space: nowrap;
}
.btn-green  { background: var(--green); color: var(--dark); }
.btn-green:hover { background: #b5f23f; }
.btn-ghost  { background: transparent; color: var(--white); border: 1.5px solid rgba(255,255,255,.35); }
.btn-ghost:hover { border-color: var(--white); }
.btn-dark   { background: var(--dark); color: var(--white); }
.btn-dark:hover { background: #1e2130; }
.btn-border-dark { background: transparent; color: var(--dark); border: 1.5px solid var(--border); }
.btn-border-dark:hover { border-color: var(--dark); }
.btn-sm { padding: 9px 20px; font-size: 13px; }

/* ============================================================
   NAVBAR
   ============================================================ */
.navbar {
  background: var(--dark);
  position: sticky;
  top: 0;
  z-index: 100;
  border-bottom: 1px solid var(--bdark);
}
.nav-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 68px;
}
.logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 15px;
  font-weight: 700;
  color: var(--white);
}
.logo-dot {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: var(--green);
}
.logo-wh { color: var(--white); }
.nav-links { display: flex; gap: 36px; }
.nav-links a {
  font-size: 14px;
  color: rgba(255,255,255,.65);
  font-weight: 500;
  transition: color .2s;
}
.nav-links a:hover { color: var(--white); }

/* ============================================================
   HERO
   ============================================================ */
.hero {
  background: var(--dark);
  padding: 90px 0 70px;
  overflow: hidden;
}
.hero-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 40px;
}
.hero-text h1 {
  font-size: 54px;
  font-weight: 800;
  line-height: 1.15;
  color: var(--white);
  margin-bottom: 22px;
}
.hero-text p {
  font-size: 15px;
  color: rgba(255,255,255,.5);
  max-width: 400px;
  margin-bottom: 34px;
  line-height: 1.75;
}
.hero-btns { display: flex; gap: 14px; }

/* 3D Blocks */
.hero-3d {
  position: relative;
  height: 320px;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  gap: 14px;
}
.blk {
  border-radius: 8px;
  position: absolute;
  box-shadow: 0 12px 30px rgba(0,0,0,.45);
}
/* Green tallest */
.b-green {
  width: 64px; height: 200px;
  background: linear-gradient(160deg, #c4f135 0%, #84cc16 100%);
  right: 180px; bottom: 0;
  transform: perspective(600px) rotateY(-12deg) rotateX(4deg);
}
/* Dark tall */
.b-dark-tall {
  width: 64px; height: 250px;
  background: linear-gradient(160deg, #3a3f52 0%, #1e2230 100%);
  right: 110px; bottom: 0;
  transform: perspective(600px) rotateY(-12deg) rotateX(4deg);
}
/* Dark short top */
.b-dark-short {
  width: 48px; height: 90px;
  background: linear-gradient(160deg, #4b5563 0%, #374151 100%);
  right: 50px; bottom: 170px;
  transform: perspective(600px) rotateY(-12deg) rotateX(4deg);
}
/* Orange */
.b-orange {
  width: 64px; height: 150px;
  background: linear-gradient(160deg, #f97316 0%, #dc2626 100%);
  right: 50px; bottom: 0;
  transform: perspective(600px) rotateY(-12deg) rotateX(4deg);
}
/* Light gray */
.b-gray {
  width: 54px; height: 120px;
  background: linear-gradient(160deg, #e5e7eb 0%, #d1d5db 100%);
  right: -10px; bottom: 0;
  transform: perspective(600px) rotateY(-12deg) rotateX(4deg);
}

/* ============================================================
   FEATURES
   ============================================================ */
.features {
  background: var(--white);
  padding: 90px 0 80px;
}
.eyebrow {
  text-align: center;
  font-size: 13px;
  color: var(--muted);
  margin-bottom: 18px;
  letter-spacing: .06em;
}
.sec-title {
  font-size: 38px;
  font-weight: 800;
  line-height: 1.25;
  text-align: center;
  color: var(--dark);
  margin-bottom: 16px;
}
.sec-sub {
  font-size: 15px;
  color: var(--muted);
  text-align: center;
  line-height: 1.75;
  margin-bottom: 52px;
}
.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}
.feat-card {
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 34px 28px;
  transition: box-shadow .2s, transform .2s;
}
.feat-card:hover {
  box-shadow: 0 10px 36px rgba(0,0,0,.07);
  transform: translateY(-3px);
}
.feat-ico { margin-bottom: 22px; }
.feat-ico svg { display: block; }
.feat-card h3 {
  font-size: 17px;
  font-weight: 700;
  margin-bottom: 12px;
}
.feat-card p { font-size: 14px; color: var(--muted); line-height: 1.7; }

/* ============================================================
   RELIABLE
   ============================================================ */
.reliable {
  background: var(--white);
  border-top: 1px solid #f0f0f0;
  padding: 80px 0;
}
.reliable-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
}
.rel-l h2 {
  font-size: 36px;
  font-weight: 800;
  line-height: 1.3;
  color: var(--dark);
}
.rel-r p { font-size: 15px; color: var(--muted); line-height: 1.8; }

/* ============================================================
   DASHBOARD
   ============================================================ */
.dash-section { padding: 0 0 90px; background: var(--white); }
.dash-box {
  display: flex;
  border-radius: 18px;
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0,0,0,.11);
  border: 1px solid var(--border);
}
.d-side {
  width: 200px;
  flex-shrink: 0;
  background: #1c1f2e;
  display: flex;
  flex-direction: column;
  padding: 0;
}
.d-brand {
  padding: 18px 20px;
  font-size: 14px;
  font-weight: 700;
  color: var(--white);
  border-bottom: 1px solid rgba(255,255,255,.07);
  display: flex;
  justify-content: space-between;
}
.d-ham { opacity: .5; font-size: 12px; }
.d-nav {
  flex: 1;
  padding: 10px 0;
  display: flex;
  flex-direction: column;
}
.d-item {
  padding: 7px 20px;
  font-size: 12px;
  color: rgba(255,255,255,.5);
  cursor: pointer;
  transition: all .15s;
  display: flex;
  align-items: center;
  gap: 7px;
}
.d-item:hover, .d-on { color: var(--white) !important; background: rgba(255,255,255,.05); }
.d-badge {
  background: #ef4444;
  color: #fff;
  font-size: 10px;
  border-radius: 10px;
  padding: 1px 6px;
  margin-left: auto;
}
.d-sec {
  font-size: 10px;
  color: rgba(255,255,255,.25);
  padding: 10px 20px 4px;
  letter-spacing: .1em;
}
.d-user {
  padding: 14px 20px;
  border-top: 1px solid rgba(255,255,255,.07);
  display: flex;
  align-items: center;
  gap: 10px;
}
.d-avt {
  width: 30px; height: 30px;
  border-radius: 50%;
  background: linear-gradient(135deg, #a3e635, #4ade80);
  flex-shrink: 0;
}
.d-un { font-size: 12px; color: var(--white); font-weight: 600; }
.d-ur { font-size: 10px; color: rgba(255,255,255,.4); }

.d-main { flex: 1; background: var(--light); display: flex; flex-direction: column; }
.d-top {
  display: flex;
  align-items: center;
  padding: 12px 20px;
  background: var(--white);
  border-bottom: 1px solid var(--border);
  gap: 12px;
}
.d-search {
  flex: 1;
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 7px 14px;
  font-size: 12px;
  color: var(--muted);
  outline: none;
  font-family: 'Sora', sans-serif;
}
.d-lang { font-size: 12px; color: var(--muted); }
.d-uavt {
  width: 28px; height: 28px;
  border-radius: 50%;
  background: linear-gradient(135deg, #6366f1, #8b5cf6);
}
.d-content {
  flex: 1;
  padding: 28px 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}
.d-content h4 { font-size: 18px; font-weight: 700; margin-bottom: 14px; }
.d-tabs { display: flex; gap: 20px; margin-bottom: 28px; }
.dt {
  font-size: 12px;
  color: var(--muted);
  cursor: pointer;
  padding-bottom: 4px;
  border-bottom: 2px solid transparent;
}
.dt-on { color: var(--dark); font-weight: 600; border-color: var(--green); }
.d-viz {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-bottom: 18px;
}
.d-server {
  width: 60px; height: 90px;
  background: linear-gradient(145deg, #3b82f6, #1d4ed8);
  border-radius: 10px;
  box-shadow: 0 10px 24px rgba(59,130,246,.35);
}
.d-nodes {
  display: grid;
  grid-template-columns: repeat(3,1fr);
  gap: 8px;
}
.d-nodes div {
  width: 24px; height: 24px;
  border-radius: 6px;
  background: linear-gradient(135deg, #60a5fa, #3b82f6);
}
.d-label2 { font-size: 15px; font-weight: 700; margin-bottom: 14px; }

/* ============================================================
   SAVE TIME
   ============================================================ */
.save-time { background: var(--dark); padding: 90px 0; }
.save-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}
.save-3d {
  position: relative;
  height: 280px;
}
.sb {
  position: absolute;
  border-radius: 8px;
  box-shadow: 0 12px 30px rgba(0,0,0,.5);
}
.sb1 {
  width: 90px; height: 170px;
  background: linear-gradient(155deg, #4b5563, #374151);
  top: 0; left: 20px;
  transform: perspective(500px) rotateY(12deg);
}
.sb2 {
  width: 90px; height: 130px;
  background: linear-gradient(155deg, #22c55e, #16a34a);
  top: 40px; left: 120px;
  transform: perspective(500px) rotateY(12deg);
}
.sb3 {
  width: 80px; height: 100px;
  background: linear-gradient(155deg, #f97316, #dc2626);
  bottom: 0; left: 60px;
  transform: perspective(500px) rotateY(12deg);
}
.save-text h2 {
  font-size: 38px;
  font-weight: 800;
  color: var(--white);
  line-height: 1.25;
  margin-bottom: 20px;
}
.save-text p {
  font-size: 14px;
  color: rgba(255,255,255,.5);
  line-height: 1.8;
  margin-bottom: 30px;
}

/* ============================================================
   INNOVATIVE
   ============================================================ */
.innovative {
  background: var(--dark);
  border-top: 1px solid var(--bdark);
  padding: 90px 0;
}
.inno-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}
.inno-l h2 {
  font-size: 36px;
  font-weight: 800;
  color: var(--white);
  line-height: 1.25;
  margin-bottom: 18px;
}
.inno-l p { font-size: 14px; color: rgba(255,255,255,.5); line-height: 1.75; margin-bottom: 30px; }
.inno-tabs {
  display: flex;
  gap: 30px;
  border-bottom: 1px solid var(--bdark);
}
.itab {
  font-size: 14px;
  color: rgba(255,255,255,.45);
  font-weight: 500;
  padding-bottom: 14px;
  border-bottom: 2px solid transparent;
  margin-bottom: -1px;
  transition: all .2s;
}
.itab:hover, .itab-on {
  color: var(--white);
  border-color: var(--green);
}
.inno-r {
  display: flex;
  justify-content: center;
  align-items: center;
}
.circles { position: relative; width: 220px; height: 220px; }
.ci {
  position: absolute;
  border-radius: 50%;
  border: 3px solid var(--dark);
}
.ci1 {
  width: 90px; height: 90px;
  background: linear-gradient(135deg,#6366f1,#8b5cf6);
  top: 0; left: 65px;
}
.ci2 {
  width: 75px; height: 75px;
  background: linear-gradient(135deg,#f59e0b,#d97706);
  bottom: 30px; left: 0;
}
.ci3 {
  width: 75px; height: 75px;
  background: linear-gradient(135deg,#ec4899,#db2777);
  bottom: 30px; right: 0;
}

/* ============================================================
   BLOCK STORAGE
   ============================================================ */
.blk-stor {
  background: var(--dark);
  border-top: 1px solid var(--bdark);
  padding: 90px 0;
}
.blk-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}
.blk-txt h2 {
  font-size: 38px;
  font-weight: 800;
  color: var(--white);
  margin-bottom: 18px;
}
.blk-txt p { font-size: 14px; color: rgba(255,255,255,.5); line-height: 1.75; margin-bottom: 28px; }
.blk-cubes { position: relative; height: 280px; }
.cu {
  position: absolute;
  border-radius: 8px;
  box-shadow: 0 10px 24px rgba(0,0,0,.4);
}
.cu1 { width: 80px; height: 80px; background: linear-gradient(145deg,#6b7280,#4b5563); top: 0; right: 90px; transform: perspective(300px) rotateX(20deg) rotateY(-20deg); }
.cu2 { width: 65px; height: 65px; background: linear-gradient(145deg,#c4f135,#84cc16); top: 40px; right: 20px; transform: perspective(300px) rotateX(20deg) rotateY(-20deg); }
.cu3 { width: 85px; height: 85px; background: linear-gradient(145deg,#f97316,#dc2626); bottom: 50px; right: 60px; transform: perspective(300px) rotateX(20deg) rotateY(-20deg); }
.cu4 { width: 60px; height: 60px; background: linear-gradient(145deg,#e5e7eb,#d1d5db); bottom: 20px; right: 5px; transform: perspective(300px) rotateX(20deg) rotateY(-20deg); }
.cu5 { width: 55px; height: 55px; background: linear-gradient(145deg,#374151,#1f2937); top: 110px; right: 150px; transform: perspective(300px) rotateX(20deg) rotateY(-20deg); }

/* ============================================================
   PRICING
   ============================================================ */
.pricing { background: var(--white); padding: 100px 0 90px; }
.price-toggle {
  display: flex;
  justify-content: center;
  gap: 0;
  margin: 0 auto 52px;
  background: #f3f4f6;
  border-radius: 10px;
  padding: 4px;
  width: fit-content;
}
.ptog {
  padding: 9px 28px;
  font-size: 14px;
  font-weight: 600;
  color: var(--muted);
  border-radius: 8px;
  cursor: pointer;
  background: transparent;
  border: none;
  font-family: 'Sora', sans-serif;
  transition: all .2s;
}
.ptog-on {
  background: var(--green);
  color: var(--dark);
}

/* THE KEY FIX: explicit 3-column grid */
.price-row {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 24px;
  align-items: start;
}

.pcard {
  background: var(--white);
  border: 1.5px solid var(--border);
  border-radius: 20px;
  padding: 36px 30px;
  display: flex;
  flex-direction: column;
  transition: box-shadow .2s;
}
.pcard:hover { box-shadow: 0 12px 40px rgba(0,0,0,.08); }

.pcard-dark {
  background: var(--dark);
  border-color: var(--dark);
  color: var(--white);
}

.pop-tag {
  background: #1e2240;
  color: var(--white);
  font-size: 12px;
  font-weight: 700;
  text-align: center;
  padding: 10px;
  margin: -36px -30px 28px;
  border-radius: 18px 18px 0 0;
  letter-spacing: .04em;
  text-transform: uppercase;
}

.p-ico { font-size: 28px; margin-bottom: 14px; }
.p-name { font-size: 18px; font-weight: 700; margin-bottom: 6px; }
.p-desc { font-size: 13px; color: var(--muted); margin-bottom: 20px; }
.pcard-dark .p-desc { color: rgba(255,255,255,.5); }

.p-price {
  font-size: 54px;
  font-weight: 800;
  line-height: 1;
  margin-bottom: 22px;
}
.p-price sup { font-size: 24px; vertical-align: top; margin-top: 14px; }
.p-price sub { font-size: 16px; font-weight: 500; color: var(--muted); }
.pcard-dark .p-price sub { color: rgba(255,255,255,.45); }

.p-list {
  list-style: none;
  margin-bottom: 28px;
  flex: 1;
}
.p-list li {
  font-size: 14px;
  color: var(--muted);
  padding: 8px 0;
  display: flex;
  align-items: center;
  gap: 10px;
}
.pcard-dark .p-list li { color: rgba(255,255,255,.65); }
.ck { color: var(--green); font-weight: 700; flex-shrink: 0; }

.p-hr { border: none; border-top: 1px solid var(--border); margin-bottom: 26px; }
.p-btn { width: 100%; }
.p-nc {
  font-size: 12px;
  color: var(--muted);
  text-align: center;
  margin-top: 12px;
}
.pcard-dark .p-nc { color: rgba(255,255,255,.35); }

/* ============================================================
   TESTIMONIALS
   ============================================================ */
.testi { background: var(--dark); padding: 100px 0 70px; text-align: center; }
.testi h2 {
  font-size: 40px;
  font-weight: 800;
  color: var(--white);
  margin-bottom: 52px;
}
.testi-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
  margin-bottom: 36px;
}
.tc {
  background: rgba(255,255,255,.04);
  border: 1px solid var(--bdark);
  border-radius: 20px;
  padding: 36px;
  text-align: left;
}
.tc-ph {
  width: 72px; height: 72px;
  border-radius: 50%;
  margin-bottom: 22px;
  background-size: cover;
}
.tc-ph1 { background: linear-gradient(135deg,#92400e,#78350f); }
.tc-ph2 { background: linear-gradient(135deg,#065f46,#064e3b); }
.tc-q {
  font-size: 14px;
  color: rgba(255,255,255,.6);
  font-style: italic;
  line-height: 1.8;
  margin-bottom: 22px;
}
.tc-nm { display: block; font-size: 15px; color: var(--white); font-weight: 700; margin-bottom: 5px; }
.tc-rl { font-size: 13px; color: rgba(255,255,255,.4); }
.testi-dots { display: flex; justify-content: center; gap: 8px; }
.td {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: rgba(255,255,255,.2);
  cursor: pointer;
  transition: all .2s;
}
.td-on { background: var(--green); width: 24px; border-radius: 4px; }

/* ============================================================
   CTA BANNER
   ============================================================ */
.cta-bar { background: var(--green); padding: 56px 0; }
.cta-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.cta-inner h2 { font-size: 34px; font-weight: 800; color: var(--dark); line-height: 1.25; }

/* ============================================================
   FOOTER
   ============================================================ */
.footer {
  background: var(--dark);
  border-top: 1px solid var(--bdark);
  padding: 28px 0;
}
.foot-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.foot-nav { display: flex; gap: 28px; }
.foot-nav a { font-size: 13px; color: rgba(255,255,255,.5); transition: color .2s; }
.foot-nav a:hover { color: var(--white); }
.foot-soc { display: flex; gap: 12px; }
.foot-soc a {
  width: 34px; height: 34px;
  border-radius: 50%;
  border: 1px solid var(--bdark);
  color: rgba(255,255,255,.5);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 700;
  transition: all .2s;
}
.foot-soc a:hover { color: var(--white); border-color: rgba(255,255,255,.3); }

/* ============================================================
   RESPONSIVE
   ============================================================ */
@media (max-width: 900px) {
  .hero-inner,
  .reliable-inner,
  .save-inner,
  .inno-inner,
  .blk-inner,
  .cta-inner { grid-template-columns: 1fr; gap: 40px; text-align: center; }

  .hero-text h1 { font-size: 36px; }
  .hero-3d { display: none; }
  .hero-btns { justify-content: center; }
  .hero-text p { max-width: 100%; }

  .feat-grid { grid-template-columns: 1fr; }

  .price-row { grid-template-columns: 1fr; max-width: 420px; margin: 0 auto; }

  .testi-row { grid-template-columns: 1fr; }

  .nav-links { display: none; }

  .foot-inner { flex-direction: column; gap: 20px; text-align: center; }

  .save-3d, .blk-cubes, .circles { display: none; }
  .save-inner, .inno-inner { grid-template-columns: 1fr; }

  .sec-title { font-size: 28px; }
  .inno-l h2, .blk-txt h2, .save-text h2 { font-size: 28px; }
}
